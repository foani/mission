#################################################
# CreataChain 기반 텔레그램 미션 게임 백엔드
# 환경변수 설정 파일 예시
#################################################

#################################################
# 기본 서버 설정
#################################################

# 애플리케이션 실행 환경 (development | production | test)
NODE_ENV=development

# 서버 포트 번호 (1-65535)
PORT=3000

# 서버 호스트 주소
HOST=0.0.0.0

#################################################
# 데이터베이스 설정 (PostgreSQL)
#################################################

# PostgreSQL 데이터베이스 연결 URL
# 형식: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://creata_user:your_password@localhost:5432/creata_mission_db

#################################################
# JWT 및 보안 설정
#################################################

# JWT 비밀 키 (32자 이상)
# 보안상 앞의한 비밀번호를 사용하세요
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long

# JWT 토큰 만료 시간
JWT_EXPIRES_IN=1h

#################################################
# CreataChain 블록체인 설정
#################################################

# Catena 메인넷 RPC URL
CATENA_RPC_URL=https://cvm.node.creatachain.com

# Catena 체인 ID (1000 = 0x3E8)
CATENA_CHAIN_ID=1000

# 에어드랍 전송용 프라이베이트 키 (0x로 시작하는 64자 헥스)
CATENA_PRIVATE_KEY=0x0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

#################################################
# 에어드랍 및 보상 설정
#################################################

# CTA 토큰 컨트렉트 주소 (40자 헥스)
CTA_TOKEN_ADDRESS=0x1234567890123456789012345678901234567890

# 최대 에어드랍 금액 (CTA 단위)
MAX_AIRDROP_AMOUNT=1000

#################################################
# Redis 설정 (캐싱용 - 선택적)
#################################################

# Redis 연결 URL (선택적 - 캐싱 기능 사용 시 필요)
# REDIS_URL=redis://localhost:6379

# Redis 비밀번호 (선택적)
# REDIS_PASSWORD=your_redis_password

#################################################
# 로깅 설정
#################################################

# 로그 레벨 (error | warn | info | debug)
LOG_LEVEL=info

# 로그 포맷 (json | pretty)
LOG_FORMAT=pretty

#################################################
# CORS 낯 API 안전 설정
#################################################

# CORS 허용 오리진 (콤마로 구분, * = 모든 오리진 허용)
CORS_ORIGINS=*

# Rate Limiting 설정
# 시간 창 (밀리초 단위, 기본 15분)
RATE_LIMIT_WINDOW_MS=900000

# 시간 창당 최대 요청 수
RATE_LIMIT_MAX_REQUESTS=100


#################################################
# CreataChain 블록체인 설정
#################################################

# 에어드랍용 관리자 지갑 프라이빗 키 (하드코딩된 비밀키)
# 보안 주의: 절대로 외부에 노출하지 마세요!
ADMIN_PRIVATE_KEY=your-admin-wallet-private-key-for-airdrops

# Catena 메인넷 RPC URL (하드코딩된 RPC 서버 정보)
CATENA_RPC_URL=https://cvm.node.creatachain.com

# Catena 체인 ID (하드코딩된 체인 설정)
CATENA_CHAIN_ID=1000

#################################################
# 텔레그램 봇 설정 (향후 확장용 - 선택적)
#################################################
# 텔레그램 봇 토큰 (BotFather에서 발급)
# TELEGRAM_BOT_TOKEN=1234567890:ABCDEFGHIJKLMNOPQRSTUVWXYZ123456789

# 텔레그램 웹훅 URL (선택적)
# TELEGRAM_WEBHOOK_URL=https://yourdomain.com/api/telegram/webhook

#################################################
# 파일 사용 지침
#################################################

# 1. 이 파일을 .env로 복사하여 사용하세요:
#    cp .env.example .env

# 2. 필수 환경변수를 설정하세요:
#    - DATABASE_URL: PostgreSQL 데이터베이스 연결 정보
#    - JWT_SECRET: 32자 이상의 보안 비밀키
#    - ADMIN_PRIVATE_KEY: 에어드랍용 관리자 지갑 프라이븏 키
#    - CATENA_RPC_URL: Catena 메인넷 RPC 서버 URL

# 3. 보안 주의사항:
#    - .env 파일은 .gitignore에 추가하여 저장소에 커밋하지 마세요
#    - 프라이베이트 키는 절대 공유하지 마세요
#    - 프로덕션에서는 강력한 JWT_SECRET을 사용하세요

# 4. 개발 환경 설정:
#    - NODE_ENV=development로 반드시 설정
#    - LOG_LEVEL=debug로 설정하여 상세 로기 확인 가능

# 5. 프로덕션 환경 설정:
#    - NODE_ENV=production
#    - LOG_LEVEL=info 또는 warn
#    - CORS_ORIGINS에 실제 도메인 명시
